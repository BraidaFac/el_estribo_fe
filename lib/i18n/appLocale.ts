/**
 * Locale unificado para HeroUI (HeroUIProvider) y formato de fechas en pantalla (Intl).
 * Cambiar solo aquí si en el futuro se usa otro país.
 */
export const APP_LOCALE = "es-AR";

/** Zona horaria para fechas con hora que vienen en ISO desde el API */
export const APP_TIME_ZONE = "America/Argentina/Buenos_Aires";
